// Copy this file to config.js and fill in your real keys.
// config.js is gitignored — never commit real keys.
export const EXERCISEDB_KEY = "";
// Get a free key at https://api.data.gov/signup (USDA FoodData Central)
export const USDA_API_KEY = "";
